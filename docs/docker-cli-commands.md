# Docker CLI Commands

- Up docker-compose
```bash
docker-compose up -d
```

- Down docker-compose
```bash
docker-compose down
```

- Stop all docker containers
```bash
docker stop $(docker ps -q)
```
